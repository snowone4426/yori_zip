function typeBanner( banner_id, photo, alt, tag_id){

    var banner_id;
    var photo;
    var alt;
    var tag_id;
    
    this.banner_id = banner_id;
    this.photo     = photo;
    this.alt       = alt;
    this.tag_id    = tag_id;
}