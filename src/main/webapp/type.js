function typeBanner( recipeId, userId, photo, alt){

    var recipeId;
    var userId;
    var photo;
    var alt;
    
    this.recipeId = recipeId;
    this.userId   = userId;
    this.photo    = photo;
    this.alt      = alt;
}